# ─────────────────────────────────────────────────────────────────────────────
# Login Module — Auto-generated GraphQL Schema Reference
# ─────────────────────────────────────────────────────────────────────────────
# This file is a REFERENCE ONLY. The actual GraphQL schema is auto-generated
# by the API Mesh OpenAPI handler from mesh/openapi.json.
#
# The mesh exposes two mutations from the LoginModule source:
#
#   mutation { otpAction(input: OtpRequest_Input!): OtpResponse }
#   mutation { customerAction(input: CustomerRequest_Input!): CustomerResponse }
#
# These are combined with the full Commerce GraphQL schema at the mesh endpoint.
#
# Config actions (getConfig, updateConfig, deleteConfig) are NOT in the mesh —
# they are admin-only and called directly from the Admin UI SDK.
# ─────────────────────────────────────────────────────────────────────────────

# ── OTP Action ───────────────────────────────────────────────────────────────
# Generate OTP:
#   mutation {
#     otpAction(input: { loginType: "mobile", mobile: "9876543210" }) {
#       otpReferenceId
#       otpValue
#     }
#   }
#
# Validate OTP:
#   mutation {
#     otpAction(input: {
#       loginType: "mobile",
#       otpReferenceId: "ref-id",
#       otpValue: "1234"
#     }) {
#       success
#       customer_token
#       message
#     }
#   }

# ── Customer Action ──────────────────────────────────────────────────────────
# Register (step 1 — triggers OTP):
#   mutation {
#     customerAction(input: {
#       operation: "register",
#       loginType: "mobile",
#       mobile: "9876543210",
#       firstname: "John",
#       lastname: "Doe"
#     }) {
#       otpReferenceId
#       otpValue
#     }
#   }
#
# Register (step 2 — validate OTP):
#   mutation {
#     customerAction(input: {
#       operation: "register",
#       loginType: "mobile",
#       mobile: "9876543210",
#       otpReferenceId: "ref-id",
#       otpValue: "1234"
#     }) {
#       success
#       customer_token
#       customer { customer_id firstname lastname email mobile_number login_type }
#     }
#   }
#
# Login (step 1 — triggers OTP):
#   mutation {
#     customerAction(input: { operation: "login", loginType: "mobile", mobile: "9876543210" }) {
#       otpReferenceId
#       otpValue
#     }
#   }
#
# Login (step 2 — validate OTP):
#   mutation {
#     customerAction(input: {
#       operation: "login",
#       loginType: "mobile",
#       otpReferenceId: "ref-id",
#       otpValue: "1234"
#     }) {
#       success
#       customerToken
#       customer { id firstname lastname email }
#     }
#   }
#
# Update Customer Details:
#   mutation {
#     customerAction(input: {
#       operation: "updateCustomerDetails",
#       customer_token: "jwt-token",
#       customer_id: 42,
#       mobile_number: "8765432109"
#     }) {
#       success
#       customer { customer_id mobile_number email login_type status }
#     }
#   }