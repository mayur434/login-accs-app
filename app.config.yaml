extensions:
  commerce/backend-ui/1:
    $include: ext.config.yaml

application:
  runtimeManifest:
    packages:
      login-module:
        license: Apache-2.0
        actions:
          otp:
            function: actions/otp/index.js
            web: 'yes'
            runtime: nodejs:20
            inputs:
              LOG_LEVEL: debug
    database:
      auto-provision: false
      region: apac